apiVersion: v1
kind: Service
metadata:
  name: notification-relay
  namespace: observability
spec:
  selector:
    app: notification-relay
  ports:
    - name: http
      port: 8080
      targetPort: 8080
